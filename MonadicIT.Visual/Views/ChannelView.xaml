<UserControl x:Class="MonadicIT.Visual.Views.ChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MonadicIT.Visual.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:ChannelViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Error probability:" 
                   Grid.Row="0" Grid.Column="0" />
        <Slider Value="{Binding ErrorProbability.Value}" 
                Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1" MinWidth="150"
                Grid.Row="0" Grid.Column="1" />
        
        <TextBlock Text="Channel capacity:" 
                   Grid.Row="1" Grid.Column="0" />
        <TextBlock Text="{Binding Path=ChannelCapacity.Value, StringFormat={}{0:F3}}" 
                   Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
